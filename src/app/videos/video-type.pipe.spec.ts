import { VideoTypePipe } from './video-type.pipe';

describe('VideoTypePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoTypePipe();
    expect(pipe).toBeTruthy();
  });
});
